---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flieCloud-db-pvc
  namespace: filecloud
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: "flieCloud-db-pv"